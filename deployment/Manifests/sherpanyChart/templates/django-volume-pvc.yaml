apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    name: static-volume
  name: static-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
